services:
  api:
    build:
      context: ../
      dockerfile: src/LatexView.Api/Dockerfile
    env_file: .env
    environment:
      - HTTP_PORTS=8080
    ports:
      - "8080:8080"
  bot:
    build:
      context: ../
      dockerfile: src/LatexView.Bot/Dockerfile
    env_file: .env
